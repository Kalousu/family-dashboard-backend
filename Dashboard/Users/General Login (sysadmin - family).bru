meta {
  name: General Login (sysadmin / family)
  type: http
  seq: 5
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "name": "Family1",
    "password": "123"
  }
}

settings {
  encodeUrl: true
}
