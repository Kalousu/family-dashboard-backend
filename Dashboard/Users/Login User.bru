meta {
  name: Login User
  type: http
  seq: 4
}

post {
  url: {{apiUrl}}/auth/authenticate
  body: json
  auth: inherit
}

body:json {
  {
    "name": "User1",
    "password": "123"
  }
}

settings {
  encodeUrl: true
}
