meta {
  name: Authenticate (Login)
  type: http
  seq: 3
}

post {
  url: {{apiUrl}}/auth/authenticate
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Kevin",
    "password": "123"
  }
}

settings {
  encodeUrl: true
}
