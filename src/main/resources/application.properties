spring.application.name=dashboard-backend
# Database Configuration
spring.datasource.url=jdbc:postgresql://141.72.176.151:5432/dashboard_db
spring.datasource.username=dashboard_user
spring.datasource.password=dashboard123
spring.datasource.driver-class-name=org.postgresql.Driver
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Spring Security
spring.security.user.name = nivek
spring.security.user.password = kevin

# JWT Configuration
spring.app.jwtSecret = ${JWT_SECRET}
spring.app.jwtExpirationMs = 86400000

# R2 Cloudflare Configuration
cloudflare.r2.endpoint = ${CLOUDFLARE_ENDPOINT}
cloudflare.r2.access-key = ${CLOUDFLARE_ACCESS_KEY}
cloudflare.r2.secret-key = ${CLOUDFLARE_SECRET_KEY}
cloudflare.r2.bucket = ${CLOUDFLARE_BUCKET}
cloudflare.r2.public-url=${CLOUDFLARE_PUBLIC_URL}

# Profile Config
spring.profiles.active=dev
